{
  "name": "purescript-lego-boost",
  "version": "0.1.0",
  "description": "Lego Boost BLE protocol bindings for Purescript",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf .cache .spago .psci_modules output .pulp-cache prod dist",
    "build": "spago build",
    "test": "spago test",
    "dev": "rimraf dist && spago install && spago build && parcel build index.html",
    "start": "spago build && parcel index.html",
    "watch": "spago build && parcel watch index.html"
  },
  "author": "Anupam Jain <ajnsit@gmail.com> (https://github.com/ajnsit)",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "purescript": "^0.12.3",
    "purescript-psa": "^0.7.3",
    "rimraf": "^2.6.3",
    "spago": "^0.7.5"
  },
  "dependencies": {
    "node-poweredup": "^2.1.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  }
}
